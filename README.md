- code_ML : code to train and evaluate ML algorithms (XGBoost and Logistic regression)
template files : use, fill and rename to fit the files needed for the code
- code_validation_PA : code to evaluate the algorithm PAstrat based on the probability previously calculated with the bootstrapped logisitic regression
